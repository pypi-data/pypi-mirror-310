[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "torbox_api"
version = "1.0.0"
license = { text = "MIT" }
description = """You can find the full documentation at https://api-docs.torbox.app!"""
readme = "PyPI_README.md"
requires-python = ">=3.7"
dependencies = [
    "requests>=2.31.0"
]
