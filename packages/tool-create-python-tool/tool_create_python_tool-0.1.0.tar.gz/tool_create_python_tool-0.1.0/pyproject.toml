[project]
name = "tool-create-python-tool"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Eyal Levin", email = "eyalev@gmail.com" }
]
requires-python = ">=3.10"
dependencies = [
    "typer>=0.13.1",
]

[project.scripts]
tool-create-python-tool = "tool_create_python_tool.cli:app"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[dependency-groups]
debug = [
    "ipython>=8.29.0",
]
