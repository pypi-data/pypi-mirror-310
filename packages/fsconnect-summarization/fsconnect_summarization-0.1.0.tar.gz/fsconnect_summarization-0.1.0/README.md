# FSConnect Summarization API

`fsconnect-summarization` is a simple API built with Flask that provides text summarization capabilities.

## Installation

To install the package, use pip:

```bash
pip install fsconnect-summarization
