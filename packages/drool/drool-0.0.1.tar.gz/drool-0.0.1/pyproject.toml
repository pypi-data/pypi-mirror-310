[tool.poetry]
name = "drool"
version = "0.0.1"
description = ""
authors = ["dm <git@defend.sh>"]
packages = [{include = "drool"}]

[tool.poetry.dependencies]
python = "^3.12"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
