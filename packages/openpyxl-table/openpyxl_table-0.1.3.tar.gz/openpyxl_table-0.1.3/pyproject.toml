[project]
name = "openpyxl-table"
version = "0.1.3"
description = "Simple wrapper for openpyxl to create tables"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "openpyxl>=3.1.5",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
