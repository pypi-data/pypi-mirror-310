[build-system]
requires = ["setuptools>=42", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "crealand"
# 正式版本号
version = "1.0.55"
# 测试版本号
# version = "1.0.9"
description = "Python SDK for the Crealand API"
authors = [
    { name = "UBTECH Robotics", email = "swenggroup@ubtrobot.com" },
]
readme = "README.md"
requires-python = ">=3.8"
dependencies = [

]
license = {file = "LICENSE"}
keywords = ["Crealand", "UBTECH Robotics Crealand"]