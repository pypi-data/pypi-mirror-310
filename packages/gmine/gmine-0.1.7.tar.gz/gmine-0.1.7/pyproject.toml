[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "gmine"
version = "0.1.7"
description = "Text mining application"
dependencies = ["pandas"]
readme = "README.md"



