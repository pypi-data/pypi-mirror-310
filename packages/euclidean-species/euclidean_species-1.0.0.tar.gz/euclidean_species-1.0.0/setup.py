from setuptools import setup, find_packages

setup(
    name='euclidean_species',
    version='1.0.0',
    packages=find_packages(),
    install_requires=[
        'pandas'
    ],
    python_requires='>=3.6',
)
