[project]
name = "listdisk"
version = "0.1.0"
description = "List Disks/Volumes/Models on windows"
readme = "README.md"
requires-python = ">=3.10"
dependencies = ["pywin32>=308", "winsys>=1.2"]

[project.optional-dependencies]
dev = [
    "pywin32-stubs>=1.0.7",
]
