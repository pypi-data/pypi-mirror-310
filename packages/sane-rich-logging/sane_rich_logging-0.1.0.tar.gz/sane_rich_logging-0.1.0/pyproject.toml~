[project]
name = "sane-rich-logging"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Jakob Guldberg Aaes", email = "jakob1379@gmail.com" }
]
requires-python = ">=3.13"
dependencies = [
    "rich>=13.9.4",
]

[project.scripts]
sane-rich-logging = "sane_rich_logging:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[dependency-groups]
dev = [
    "pre-commit>=4.0.1",
    "pytest>=8.3.3",
    "ruff>=0.7.4",
]
