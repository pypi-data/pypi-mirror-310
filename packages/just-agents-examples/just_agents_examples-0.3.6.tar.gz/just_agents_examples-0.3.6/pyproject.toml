[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "just-agents-examples"
version = "0.3.6"
description = "Just Agents - Example Components"
readme = "README.md"
requires-python = ">=3.10"
authors = [
    { name = "Alex Karmazin", email = "karmazinalex@gmail.com" },
]
maintainers = [
    { name = "Anton Kulaga", email = "antonkulaga@gmail.com" },
]
dependencies = [
    "just-agents>=0.3.6",
    "just-agents-coding>=0.3.6",
    "just-agents-router>=0.3.6",
    "just-agents-web>=0.3.6",
    "just-agents-tools>=0.3.6",
]
license = {text = "MIT"}

[tool.setuptools.packages.find]
where = ["."]
include = ["src*"]
exclude = [
    "logs*",
    "output*", 
    "*.egg-info",
    "build",
    "dist",
    "__pycache__",
]

[tool.setuptools.package-data]
"config" = ["*.yaml", "*.yml"]
"just_agents_examples" = ["*.yaml", "*.yml"] 