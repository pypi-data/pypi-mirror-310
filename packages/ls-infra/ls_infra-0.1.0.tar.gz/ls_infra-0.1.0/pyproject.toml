[build-system]
requires = ["setuptools>=69.0"]
build-backend = "setuptools.build_meta"

[project]
name = "ls-infra"
version = "0.1.0"
description = "Infrastructure management tools"
requires-python = ">=3.12"
dependencies = [
    # Add your runtime dependencies here if needed
]
